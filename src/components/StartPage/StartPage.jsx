import React, { Fragment } from 'react';

const StartPage = (props) => {
    console.log('StartPage')
    return (
        <Fragment>
            <td>Привет</td>
            <td>Мир</td>
        </Fragment>
    );
};

export default StartPage;