import React, { Fragment } from 'react';

const StartPage = (props) => {
    return (
        <Fragment>
            <td>Привет</td>
            <td>Мир</td>
        </Fragment>
    );
};

export default StartPage;